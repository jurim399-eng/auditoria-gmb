[build]
  publish = "."
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"

# El frontend llama a /api/audit; Netlify lo redirige a la function real.
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
